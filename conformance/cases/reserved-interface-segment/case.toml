name = "reserved-interface-segment"
description = "Java keywords as WIT interface names are mangled in nested-style interface package segments (class -> class_, default -> default_); accessors mangle too (spec sections 3.1, 3.3)"
options = ["--no-support"]
